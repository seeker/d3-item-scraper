sudo: false
language: python
python:
    - "3.6"
install:
  pip install -e .[dev]

jobs:
  include:
    - stage: test
      script: ./cover.sh
      after_success: bash <(curl -s https://codecov.io/bash)
